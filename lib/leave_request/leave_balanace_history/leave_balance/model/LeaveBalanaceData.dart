class LeaveBalanceData{
  int id;
  String title;
  String consumed;
  String quota;

  LeaveBalanceData(this.id, this.title, this.consumed, this.quota);
}
List<LeaveBalanceData> balanceData=[
 // LeaveBalanceData(1, '', consumed, quota)
];